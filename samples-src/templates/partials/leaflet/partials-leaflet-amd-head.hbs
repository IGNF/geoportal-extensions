        <!-- requirejs -->
        <script src="{{ baseurl }}/node_modules/requirejs/require.js"></script>
        <script>
            /* global requirejs */
            requirejs.config({
                'paths': {
                    "leaflet" : "{{ baseurl }}/node_modules/leaflet/dist/leaflet{{ mode }}",
                    "Gp"      : "{{ baseurl }}/dist/leaflet/GpPluginLeaflet{{ mode }}"
                },
                shim : {
                    "Gp" : ["leaflet"]
                }
            });
        </script>
